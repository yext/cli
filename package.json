{
  "name": "cli",
  "version": "1.0.0",
  "description": "Command-line Interface for the Yext Platform",
  "main": "src/index.js",
  "author": "jwartofsky@yext.com",
  "license": "BSD-3-Clause",
  "type": "module",
  "bin": {
    "yext": "src/cli.js"
  },
  "engines": {
    "node": ">=17"
  },
  "dependencies": {
    "rimraf": "^5.0.5",
    "bin-wrapper": "^4.1.0"
  },
  "scripts": {
    "postinstall": "rimraf vendor && node src/setup/install",
    "generate-notices": "generate-license-file --input package.json --output ./THIRD-PARTY-NOTICES --overwrite && node scripts/license_append.js",
    "fmt": "prettier --write --cache .",
    "lint": "eslint --cache --fix ."
  },
  "devDependencies": {
    "eslint": "^8.51.0",
    "eslint-plugin-import": "^2.28.1",
    "generate-license-file": "^2.0.0",
    "prettier": "^3.0.3"
  },
  "packageManager": "pnpm@8.8.0"
}